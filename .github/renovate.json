{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    ":semanticCommits",
    ":prHourlyLimitNone"
  ],
  "timezone": "Australia/Perth",
  "schedule": [
    "after 10pm every weekday",
    "before 6am every weekday",
    "every weekend"
  ],
  "dependencyDashboardTitle": "Dependency dashboard: Renovate",
  "labels": ["dependencies"],
  "rangeStrategy": "bump",
  "prConcurrentLimit": 20,
  "minimumReleaseAge": "3 days",
  "vulnerabilityAlerts": {
    "enabled": true,
    "schedule": ["at any time"],
    "labels": ["dependencies", "security"]
  },
  "packageRules": [
    {
      "description": "One PR per dep — matches existing per-dependency Linear-ticket cadence",
      "matchManagers": ["cargo", "npm", "dockerfile", "docker-compose", "github-actions"],
      "groupName": null
    },
    {
      "description": "Auto-merge patch updates for GitHub Actions and Docker base images (low-risk regardless of version)",
      "matchManagers": ["github-actions", "dockerfile", "docker-compose"],
      "matchUpdateTypes": ["patch", "pin", "digest"],
      "automerge": true,
      "automergeType": "pr",
      "platformAutomerge": true
    },
    {
      "description": "Auto-merge patch updates for stable (v1.0.0+) cargo and npm dependencies. Pre-v1.0 (0.x.y) deps stay manual — SemVer isn't binding below 1.0 and Rust/npm 0.x patches can include behavioural changes",
      "matchManagers": ["cargo", "npm"],
      "matchUpdateTypes": ["patch"],
      "matchCurrentVersion": "!/^0\\./",
      "automerge": true,
      "automergeType": "pr",
      "platformAutomerge": true
    },
    {
      "description": "Pin rand_core_06 cargo alias at v0.6 — the alias name explicitly documents the deliberate version pin (v0.6 API is required by a transitive crypto consumer). Any bump silently breaks the pin intent and gets closed manually (see PR #122). Revisit if the alias is removed from Cargo.toml",
      "matchManagers": ["cargo"],
      "matchDepNames": ["rand_core_06"],
      "enabled": false
    },
    {
      "description": "Hold eslint and @eslint/js at v9 until eslint-plugin-react ships v10-compatible release. eslint-plugin-react@7.37.5 (13+ months old, jsx-eslint/eslint-plugin-react#3977 stale) declares peer eslint@\"^3 || ^4 || ^5 || ^6 || ^7 || ^8 || ^9.7\" — npm install errors with ERESOLVE on any v10 bump. PRs #135 and #136 closed for this reason. Migration off eslint-plugin-react to @eslint-react/eslint-plugin tracked in a follow-up ADR; remove this rule once that ADR is implemented",
      "matchManagers": ["npm"],
      "matchPackageNames": ["eslint", "@eslint/js"],
      "allowedVersions": "<10"
    }
  ]
}
